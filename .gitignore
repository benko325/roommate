# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example

# Prisma
prisma/*.db
prisma/*.db-journal

# Note: the Kubb-generated API client (frontend/src/lib/api/generated) and the
# TanStack Router route tree (frontend/src/routeTree.gen.ts) are committed so
# builds/type-checks are hermetic (CI needs them before any dev server runs).
# Regenerate with `pnpm generate:api` / automatically during `vite dev|build`.

# Turbo
.turbo/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Coverage / test output
coverage/
.vitest/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Local Claude skills (kept local, not version-controlled)
.claude/skills/

# Misc
*.local
